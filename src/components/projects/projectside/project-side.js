import React from "react";
import "./project-side.scss";

const ProjectSide = ({title}) => {
    return(
        <section>
            <h1>{title}</h1>
        </section>
    );
};
export default ProjectSide;